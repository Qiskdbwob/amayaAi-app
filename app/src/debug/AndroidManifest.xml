<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Debug-only ADB harness. Export stays intentional so `am start` can launch it;
             release and benchmark manifests contain no matching activity. -->
        <activity
            android:name="com.amaya.intelligence.ui.activities.browser.BrowserDebugActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@style/Theme.Amaya" />
    </application>
</manifest>
